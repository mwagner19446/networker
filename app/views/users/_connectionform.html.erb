Manually Add New Connections
<%= form_for [@user, @connection] do |f| %>
  <br \>
  <%= f.label :c_type, ('Connection Type') %>
  <%= f.select :c_type, options_for_select(["Connector","Mentor","Peer"]) %> 
  <br \>
  <%= f.label :name %>
  <%= f.text_field :name %> 
  <br \>
  <%= f.submit %> 
<% end %>