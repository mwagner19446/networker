<table >
  <tr>
    <th></th>
    <th>Type</th>
    <th>Title</th>
    <th>Notes</th>
  </tr>
  <% @tasks.each do |task| %>
  <tr>
    <td><%=link_to image_tag("delete.png",size: "20x20"), user_task_path(params[:id],task.id), method: "delete" %></td>
    <td><%=task.t_type%></td>
    <td><%=task.title%></td>
    <td><%=task.notes%></td>
  </tr> 
  <% end %>
</table>