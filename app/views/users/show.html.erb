<div id="dash_wrapper">
  <div id="person_header">
    <h1>  
      <%if @user.photo_url ==""%>
        <%= image_tag("default_photo.jpg", size: "80x80") %>
        <%else%>
         <img src="<%=@user.photo_url%>" width="80" height="80">
      <%end%>
      <%=@user.name%>
      <%=link_to image_tag("edit.png"), edit_user_path(@user.id)%>
    </h1>
  </div>

  <div id="action_panel">
    <div id="task_panel">
      <div class="task_table">
        <h2>User Tasks: </h2>
        <%= render "taskfigure" %>
        <%= render "taskform" %>        
      </div>
      <div class="hint">
        <div class="hint_border"><h4>HINT:</h4></div> 
        <div class="hint_body">
          <p>Network to identity opportunities</p>
          <p>Create Actionable Tasks for you to reach your goals</p>
        </div>
      </div>
    </div>
    <div id="connection_panel">
    <h2>User Connections:</h2> 
      <div class="connection_table">
        <h3>Connectors</h3>
        <%= render "connectiontiles_connectors" %>
        <h3>Mentors</h3>
        <%= render "connectiontiles_mentors" %>
        <h3>Peers</h3>
        <%= render "connectiontiles_peers" %>
      </div>
      <div class="hint">
        <div class="hint_border"><h4>HINT:</h4></div> 
        <div class="hint_body">
          <p>CONNECTORS will introduce you to other hiring managers.  (Think Former Bosses)</p>
          <p>MENTORS have formerly done your job and are now doing the job you want.  Look to them for career path guidance.</p>
          <p>PEERs currently do the same job as you.  Look to them to develop your craft.</p>
        </div>
      </div>
    </div>  
  </div>

  <div id="nav_panel">
    <div class="partner">
      Search our Partners: 
      <%=link_to image_tag("meetup.png"), user_events_path(@user.id) %>
    </div> 
    <%= render "connectionform" %>
    <div class="partner">
      Search our Partners: 
      <a href="/users/<%= @user.id %>/liusers"><%=image_tag("LinkedIn.png", size: "30x30")%></a>
      <%=link_to image_tag("meetup.png"), user_groups_path(@user.id) %>
    </div>
  </div>

</div>

